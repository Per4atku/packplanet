# PostgreSQL Database Configuration
# IMPORTANT: These variables are used by the postgres service
POSTGRES_DB=packplanet
POSTGRES_USER=packplanet_user
POSTGRES_PASSWORD=change_this_secure_password_in_production

# Database URL for Prisma
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
# CRITICAL: Host must be 'postgres' (Docker service name), NOT 'localhost'
# In Docker Compose, services communicate using service names as hostnames
DATABASE_URL=postgres://packplanet_user:change_this_secure_password_in_production@postgres:5432/packplanet

# Admin Panel Authentication
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_secure_admin_password

# GitHub Container Registry (for docker-compose.yml)
# Format: owner/repository (e.g., username/packplanet or org/packplanet)
GITHUB_REPOSITORY=your-github-username/packplanet

# Node Environment
NODE_ENV=production
