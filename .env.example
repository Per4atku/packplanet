# ============================================
# Environment Variables for Pack Planet
# ============================================
# Copy this file to .env and fill in the values

# ============================================
# Database Configuration
# ============================================
# For local development (direct PostgreSQL connection):
# DATABASE_URL="postgresql://username:password@localhost:5432/packplanet?schema=public"
#
# For Docker deployment (use docker service name as host):
# DATABASE_URL="postgresql://postgres:your-password@postgres:5432/packplanet?schema=public"
#
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/packplanet?schema=public"

# PostgreSQL Configuration (for Docker Compose)
# These are used by the postgres container
POSTGRES_DB=packplanet
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_secure_password

# ============================================
# Application Configuration
# ============================================
# Site URL for SEO metadata (Open Graph, Twitter Cards, sitemap, etc.)
# Use your production domain in production (e.g., https://packplanet.ru)
# Use http://localhost:3000 for local development
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Node Environment (development | production)
NODE_ENV=development

# Disable Next.js telemetry (optional)
NEXT_TELEMETRY_DISABLED=1

# Application Port (default: 3000)
PORT=3000
